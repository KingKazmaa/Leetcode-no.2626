const reduce = (nums, fn, init) => {
    let res = init;
    if (nums.length == 0) {
        return init
    } else {
    for (let i=0; i < nums.length; i++) {
res = fn(res, nums[i])
    }
    } return res
};
